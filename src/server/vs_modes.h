

#define RAW_VECTOR_MODE            0  /* vectors evaluated on raw dimensions               */
#define CLASSIFIED_VECTOR_MODE     2  /* thesaurus adds dimensions of classification       */
#define META_VECTOR_MODE           4  /* vector dims removed, class info ONLY is vectored. */
