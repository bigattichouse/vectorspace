grid manager sits accepts client requests, has conf file to manage others. special command to recheck the lists.
inserts are round robin, deletes are broadcast, queries are broadcast and aggregated.

